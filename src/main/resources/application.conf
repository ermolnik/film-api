ktor {
    application {
        modules = [ com.ermolnik.ApplicationKt.module ]
    }

    db-driver = "org.h2.Driver"

    db-source = "jdbc:h2:./db/movie_tracker"
    db-source = ${?DB_SOURCE}

    db-user = "root"
    db-password = ""

    deployment {
        host = "0.0.0.0"
        host = ${?HOST}

        port = "8080"
        port = ${?PORT}
    }
}
jwt {
    audience = "film-audience"
    issuer = "http://localhost/"
    realm = "Film realm"
    secret = "12341567854399043442314" // need to remove hardcore value and change to required value
}